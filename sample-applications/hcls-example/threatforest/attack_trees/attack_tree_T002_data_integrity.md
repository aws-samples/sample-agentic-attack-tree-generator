# Attack Tree: Data Integrity

**Threat ID**: T002  
**Associated threat statement**: T002 - Data Integrity

- **Threat Source**: cybercriminal
- **Prerequisites**: compromised IAM credentials
- **Threat Action**: manipulate data processing workflows in AWS Glue
- **Threat Impact**: corruption of healthcare analytics datasets
- **Reduced Goal**: integrity
- **Impacted Assets**: data warehouse information
- **Priority**: High
- **Category**: Data Integrity

---

## Attack Tree Diagram

```mermaid
graph TD
    Start["Cybercriminal with compromised IAM credentials"]
    Start --> Recon["Reconnaissance of AWS Glue environment"]
    Recon --> Path1A["Identify ETL jobs and workflows"]
    Recon --> Path2A["Enumerate data catalog and schemas"]
    Path1A --> Attack1["Modify existing Glue ETL scripts"]
    Path1A --> Attack2["Create malicious Glue jobs"]
    Path1A --> Attack3["Alter job parameters and configurations"]
    Path2A --> Attack4["Modify Glue Data Catalog metadata"]
    Path2A --> Attack5["Change table schemas and mappings"]
    Attack1 --> Exec1["Inject data transformation logic"]
    Attack2 --> Exec2["Schedule unauthorized data processing"]
    Attack3 --> Exec3["Point jobs to malicious data sources"]
    Attack4 --> Exec4["Corrupt schema definitions"]
    Attack5 --> Exec5["Alter column mappings"]
    Exec1 --> Impact["Corrupt healthcare analytics datasets"]
    Exec2 --> Impact
    Exec3 --> Impact
    Exec4 --> Impact
    Exec5 --> Impact
    Impact --> Goal["Compromise integrity of data warehouse information"]
    classDef attack fill:#ffcccc
    classDef goal fill:#ffcc99
    classDef fact fill:#ccccff
    class Recon,Path1A,Path2A,Attack1,Attack2,Attack3,Attack4,Attack5,Exec1,Exec2,Exec3,Exec4,Exec5,Impact attack
    class Goal goal
    class Start fact
    classDef mitigation fill:#ccffcc
```

## Attack Path Analysis

This attack tree represents the potential attack paths for the identified threat. Each node in the tree represents either:
- **Attack Goal** (orange): The ultimate objective
- **Attack Step** (red): Individual attack actions
- **Fact/Condition** (blue): Prerequisites or conditions
- **Mitigation** (green): Defensive measures

Review this attack tree to:
1. Identify critical attack paths
2. Implement appropriate security controls
3. Monitor for indicators of these attack patterns
4. Develop incident response procedures

---
*Generated by ThreatForest - Attack Tree Analysis*
